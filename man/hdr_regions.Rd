% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hdr.R
\name{hdr_regions}
\alias{hdr_regions}
\title{Highest density regions for each observation}
\usage{
hdr_regions(object, prob)
}
\arguments{
\item{object}{A \code{dist_kde} object, as returned by \code{dist_kde()}, containing
a single distribution estimated from univariate or bivariate data.}

\item{prob}{A numeric vector of probabilities giving the HDR coverage
(between 0 and 1).}
}
\value{
A tibble containing the data used to estimate \code{object},
along with one additional integer
column per element of \code{prob} (named \verb{hdr_<100*prob>}) showing which region
of the corresponding HDR each observation falls in.
}
\description{
For a \code{dist_kde} object, determine which highest density region (HDR)
each observation falls in, for one or more coverage probabilities. Some
densities are multimodal, so the HDR for a given \code{prob} can consist of
several disjoint regions; the returned columns identify the specific
region (\code{1}, \code{2}, ...) that each observation falls in, ordered from
lowest to highest along the first variable, with \code{NA} for observations
outside the HDR at that \code{prob}.
}
\examples{
dist_kde(oldfaithful$duration) |> hdr_regions(c(0.5, 0.95))
dist_kde(oldfaithful[, c("duration", "waiting")]) |> hdr_regions(0.90)
}
\seealso{
\code{\link{hdr_table}}, \code{\link{gg_hdrboxplot}}
}
\author{
Rob J Hyndman
}
