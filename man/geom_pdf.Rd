% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_pdf.R
\name{geom_pdf}
\alias{geom_pdf}
\alias{StatPdf}
\alias{geom_pdf_2d}
\title{Probability density function geom}
\usage{
geom_pdf(
  mapping = NULL,
  data = NULL,
  dist,
  scale = 1,
  ngrid = 501,
  geom = "line",
  position = "identity",
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

geom_pdf_2d(
  mapping = NULL,
  data = NULL,
  dist,
  ngrid = 101,
  prob = seq(0.1, 0.9, by = 0.1),
  filled = TRUE,
  ...,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings. Usually \code{NULL}; when \code{length(dist) > 1}
and \code{mapping} is \code{NULL}, \code{colour} is automatically mapped to the computed
\code{distribution} variable.}

\item{data}{A data frame. Usually \code{NULL}; the layer does not need user data.}

\item{dist}{A distribution object from the distributional package, or from
\code{\link[=dist_kde]{dist_kde()}}.}

\item{scale}{Scaling factor applied to the density.}

\item{ngrid}{Number of grid points at which the density is evaluated.}

\item{geom}{The geometric object used to display the layer. Defaults to
\code{"line"}; alternatives such as \code{"area"} or \code{"path"} also work.}

\item{position}{Position adjustment, defaults to \code{"identity"}.}

\item{...}{Other arguments passed to the underlying geom (e.g. \code{linetype},
\code{linewidth}, \code{colour}, \code{alpha}).}

\item{na.rm}{Passed through to the underlying geom.}

\item{show.legend}{Logical; should this layer be included in legends?}

\item{inherit.aes}{Logical; if \code{FALSE}, overrides the default aesthetics.}

\item{prob}{Coverage probabilities for the highest density regions used as
contour breaks. Defaults to \code{seq(0.1, 0.9, by = 0.1)}.}

\item{filled}{Logical. If \code{TRUE} (default), draw filled HDR bands using
\code{\link[ggplot2:geom_contour_filled]{ggplot2::geom_contour_filled()}}. If \code{FALSE}, draw unfilled contour lines
using \code{\link[ggplot2:geom_contour]{ggplot2::geom_contour()}}.}
}
\value{
A ggplot2 layer.
}
\description{
A ggplot2 layer that displays the probability density function (pdf) of a
\link[distributional:distributional]{distributional} object. For univariate distributions it draws a line plot
of the density; for bivariate distributions, use \code{\link[=geom_pdf_2d]{geom_pdf_2d()}}.
}
\details{
Unlike most geoms, \code{geom_pdf()} does not consume data through the standard
data/aesthetic pipeline. Instead, the distribution is supplied through the
\code{dist} parameter and the underlying \link{StatPdf} generates the plotting data
from it. The computed columns \code{x}, \code{density}, and \code{distribution} are
available via \code{\link[ggplot2:after_stat]{ggplot2::after_stat()}}.
}
\examples{
\dontrun{
library(ggplot2)
library(distributional)

# ----- Univariate -----
mix <- dist_mixture(
  dist_normal(-2, 1),
  dist_normal(2, 1),
  weights = c(1 / 3, 2 / 3)
)

ggplot() +
  geom_pdf(dist = mix) +
  geom_pdf(dist = dist_normal(-2, 1), scale = 1 / 3, linetype = "dashed") +
  geom_pdf(dist = dist_normal(2, 1),  scale = 2 / 3, linetype = "dashed")

# Multiple distributions are auto-coloured
ggplot() +
  geom_pdf(dist = c(dist_normal(0, 1), dist_student_t(df = 3)))

# ----- Bivariate -----
biv <- dist_multivariate_normal(
  mu    = list(c(0, 0)),
  sigma = list(matrix(c(1, 0.6, 0.6, 1), nrow = 2))
)

# Default: filled HDR bands
ggplot() +
  geom_pdf_2d(dist = biv) +
  coord_equal()

# Just contour lines, no fill
ggplot() +
  geom_pdf_2d(dist = biv, filled = FALSE) +
  coord_equal()

# Contour lines coloured by HDR level, with a custom subset of probabilities
ggplot() +
  geom_pdf_2d(
    dist    = biv,
    filled  = FALSE,
    prob    = c(0.5, 0.8, 0.95),
    mapping = aes(x = x, y = y, z = Density, colour = after_stat(level))
  ) +
  coord_equal()
}
}
\author{
Rob J Hyndman
}
