% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment.R
\name{augment.Pca}
\alias{augment.Pca}
\title{Augment data with results from a robust principal component analysis}
\usage{
\method{augment}{Pca}(x, data = NULL, ...)
}
\arguments{
\item{x}{An rrcov \verb{Pca*} object.}

\item{data}{The original data matrix or data frame used to compute the PCA.
If supplied, its columns are bound to the left of the returned tibble.}

\item{...}{Unused.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per observation.
}
\description{
Augment the data with information from an rrcov \verb{Pca*} object
(such as the output of \code{\link[rrcov:PcaHubert]{rrcov::PcaHubert()}} or \code{\link[rrcov:PcaClassic]{rrcov::PcaClassic()}}). The
returned tibble contains the principal component scores (\code{.fittedPC1},
\code{.fittedPC2}, ...), the score distance (\code{.sd}) and the orthogonal distance
(\code{.od}) of each observation. The score distance measures how far an
observation lies from the centre \emph{within} the projection subspace, while the
orthogonal distance measures how far it lies \emph{from} the subspace. If \code{data}
is supplied, its columns are returned alongside these results.
}
\examples{
\dontshow{if (requireNamespace("rrcov", quietly = TRUE)) withAutoprint(\{ # examplesIf}
Y <- oldfaithful[, c("duration", "waiting")]
pca <- rrcov::PcaHubert(as.matrix(Y), k = 1)
broom::augment(pca, data = Y)
\dontshow{\}) # examplesIf}
}
\author{
Rob J Hyndman
}
