% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outlier_map.R
\name{outlier_map}
\alias{outlier_map}
\title{Outlier map from a projection or principal component analysis}
\usage{
outlier_map(
  object = NULL,
  data = NULL,
  scores = NULL,
  loadings = NULL,
  show_thresholds = TRUE,
  ...
)
}
\arguments{
\item{object}{Optionally, the output of \code{\link[stats:prcomp]{stats::prcomp()}} or an rrcov \verb{Pca*}
function. For a \code{prcomp} object, set the number of retained components with
its \code{rank.} argument. If supplied, the \code{scores} and \code{loadings} arguments are
ignored.}

\item{data}{The original data matrix or data frame used to compute the
projection, scaled if the projection was computed on scaled data. This is
required to compute the orthogonal distances, except when \code{object} is a
rrcov \verb{Pca*} object (which stores them).}

\item{scores}{A matrix or data frame of scores, with one column per retained
component. Ignored if \code{object} is supplied.}

\item{loadings}{A matrix or data frame of loadings, with one column per
retained component. Ignored if \code{object} is supplied.}

\item{show_thresholds}{If \code{TRUE} (the default) and \code{object} is a PCA-like
object, the score-distance and orthogonal-distance cutoffs are drawn as
dashed lines and observations are coloured by type. Ignored when \code{scores}
and \code{loadings} are passed directly.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw an outlier map showing the score distance and orthogonal
distance of each observation from a projection or principal component
analysis. The score distance measures how far an observation lies from the
centre \emph{within} the projection subspace, while the orthogonal distance
measures how far it lies \emph{from} the subspace. Pass \code{object} (the output of
\code{\link[stats:prcomp]{stats::prcomp()}} or an rrcov \verb{Pca*} function); otherwise supply \code{scores} and
\code{loadings} together with the original \code{data}. For a \code{prcomp} object, use its
\code{rank.} argument to set the number of retained components.

When \code{object} is a PCA-like object and \code{show_thresholds = TRUE}, the
score-distance and orthogonal-distance cutoffs are drawn as dashed lines and
observations are coloured by type:
\describe{
\item{Regular observation}{small score and orthogonal distance.}
\item{Good leverage point}{large score distance, small orthogonal distance.}
\item{Orthogonal outlier}{small score distance, large orthogonal distance.}
\item{Bad leverage point}{large score and orthogonal distance.}
}
The cutoffs are only defined for PCA-like objects, so \code{show_thresholds} is
ignored when \code{scores} and \code{loadings} are passed directly.
}
\examples{
Y <- oldfaithful[, c("duration", "waiting")]
prcomp(Y, scale = TRUE, rank. = 1) |>
  outlier_map(data = Y)
}
\references{
Hyndman, R J (2026) "That's weird: Anomaly detection using R",
Chapter 9, \url{https://OTexts.com/weird/}.
}
\author{
Rob J Hyndman
}
