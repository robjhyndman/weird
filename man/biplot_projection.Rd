% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biplot_projection.R
\name{biplot_projection}
\alias{biplot_projection}
\title{Biplot of a two-dimensional projection}
\usage{
biplot_projection(
  object = NULL,
  scores = NULL,
  loadings = NULL,
  label_threshold = 0,
  arrow_colour = "#c14b14",
  ...
)
}
\arguments{
\item{object}{Optionally, the output of \code{\link[stats:prcomp]{stats::prcomp()}} or an \verb{rrcov::Pca*}
function. If supplied, the scores and loadings are extracted from it and the
\code{scores} and \code{loadings} arguments are ignored.}

\item{scores}{A matrix or data frame of scores centred about the origin,
with the first two columns used as the horizontal and vertical coordinates.
Ignored if \code{object} is supplied.}

\item{loadings}{A matrix or data frame of loadings, with row names giving the
variable names and the first two columns used as the arrow directions.
Ignored if \code{object} is supplied.}

\item{label_threshold}{Only loadings whose absolute length exceeds this
threshold are labelled. The default of \code{0} labels every non-zero loading.}

\item{arrow_colour}{Colour of the arrows and labels.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw a two-dimensional projection of the scores with the
original variable axes overlaid as arrows (loadings), as in a biplot. Pass
\code{object} (the output of \code{\link[stats:prcomp]{stats::prcomp()}} or an \verb{rrcov::Pca*} function);
otherwise supply \code{scores} and \code{loadings} directly. All scores should be
centred about the origin. The arrows are stretched by a common factor so that
the longest arrow just reaches the edge of the point cloud, and only loadings
longer than \code{label_threshold} are labelled.
}
\examples{
oldfaithful[, c("duration", "waiting")] |>
  prcomp(scale = TRUE) |>
  biplot_projection()
}
\references{
Hyndman, R J (2026) "That's weird: Anomaly detection using R",
Chapter 9, \url{https://OTexts.com/weird/}.
}
\author{
Rob J Hyndman
}
