% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biplot_projection.R
\name{biplot_projection}
\alias{biplot_projection}
\title{Biplot of a two-dimensional projection}
\usage{
biplot_projection(
  object = NULL,
  scores = NULL,
  loadings = NULL,
  label_threshold = 0
)
}
\arguments{
\item{object}{Optionally, the output of \code{\link[stats:prcomp]{stats::prcomp()}} or an rrcov \verb{Pca*}
function. If supplied, the scores and loadings are extracted from it and the
\code{scores} and \code{loadings} arguments are ignored.}

\item{scores}{A matrix or data frame of scores, with the first two columns
used as the horizontal and vertical coordinates. Ignored if \code{object} is
supplied.}

\item{loadings}{A matrix or data frame of loadings, with row names giving the
variable names and the first two columns used as the arrow directions.
Ignored if \code{object} is supplied.}

\item{label_threshold}{Only loadings whose squared length exceeds this
threshold are labelled. The default of \code{0} labels every loading.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw a two-dimensional projection of the scores with the
original variable axes overlaid as arrows (loadings), as in a biplot. Pass
\code{object} (the output of \code{\link[stats:prcomp]{stats::prcomp()}} or an rrcov \verb{Pca*} function) to take
the scores and loadings from it; otherwise supply \code{scores} and \code{loadings}
directly. The arrows are stretched by a common factor so that the longest
arrow just reaches the edge of the point cloud, and only loadings longer than
\code{label_threshold} are labelled.
}
\examples{
pca <- prcomp(oldfaithful[, c("duration", "waiting")], scale = TRUE)
biplot_projection(pca)
}
\author{
Rob J Hyndman
}
