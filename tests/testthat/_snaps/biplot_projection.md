# biplot_projection errors for an unsupported object

    Code
      biplot_projection(object = lm(v1 ~ v2, df_bivar))
    Condition
      Error in `biplot_projection()`:
      ! object must be the output of prcomp() or an rrcov::Pca* function

