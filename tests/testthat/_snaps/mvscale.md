# mvscale errors on Inf in a vector

    Code
      mvscale(v_inf, warning = FALSE)
    Condition
      Error in `mvscale()`:
      ! object contains infinite values

# mvscale errors on Inf in a matrix

    Code
      mvscale(mat_inf, warning = FALSE)
    Condition
      Error in `mvscale()`:
      ! object contains infinite values

# mvscale errors on Inf in a data frame

    Code
      mvscale(df_inf, warning = FALSE)
    Condition
      Error in `mvscale()`:
      ! object contains infinite values

